Build a tiny HTTP inventory service in `main.go` only.

Requirements:
- Keep everything in one file.
- Add `GET /items` and `POST /items`.
- Add `GET /healthz`.
- Return JSON everywhere.
- Keep the implementation runnable with `go run .`.
- Verify with `go build ./...` before finishing.

Do not add more files unless you absolutely have to.
